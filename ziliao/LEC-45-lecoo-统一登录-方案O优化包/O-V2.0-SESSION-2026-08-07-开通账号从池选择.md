# 会话：开通账号改为从账号池选择

- 日期：2026-08-07
- 范围：`leuc-proto`

## 需求

系统负责人「开通账号」时，业务系统账号名改为**从「业务系统管理 · 全部账户」选择**（不可手填新建）；默认展示无归属，可切换查看全部并支持查询弹窗。

## 改动

1. `static/index.html`：开通弹窗改为只读选择 + `m-prov-pick` 查询弹窗（默认仅无归属）。
2. `provision_account_apply`：支持 `account_id`；必须命中该系统账号池，不再按名自动建池账号。
3. 待办审批校验文案改为「请从账号池选择」。

## 验证

- 来酷ERP `catalog?unbound_only=1` → 无归属 7。
