# 会话记录 · 2026-08-06 · 安全管理

## 目标

个人中心「安全管理」补齐：密码、手机/邮箱改绑、账号有效期、人脸/指纹、SSO。

## 改动

- `leuc-proto/db.py`：`users` 增加 `account_expire`、人脸/指纹录入字段与种子数据
- `leuc-proto/app.py`：`ensure_db` / `row_user`；新增 `/api/security/*`
- `leuc-proto/static/index.html`：安全管理页完整交互 + `m-sec` 弹窗
- `V2.0/用户中心功能模块整理-O-V2.1.md`、`O-V2.0-UPDATE-RECORD.md` 同步

## 增量（同日）· 账号延期

- 安全管理中账号有效期改为**只读**，去掉自行设置/清空
- 自助申请「密码延期」→「账号延期」；审批通过延长 `account_expire`
- 兼容旧类型 `password_extend` / 待办「密码延期」

## 增量（同日）· 账号申请选人弹窗

- 「账号申请」：弹窗按组织树选人（可追加）→ 多选系统写入明细（可重复）
- 提交 `items` 明细；一人可多业务系统账号
- 自助申请「账号申请」同步为明细追加模式

## 手测

1. `wangqiang` → 我的组织 → 账号申请 → 追加选人（组织树筛选）→ 勾选系统「追加到明细」（可再点一次重复）→ 提交
2. API：同一人重复同一系统可生成多条申请
