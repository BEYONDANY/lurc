# 会话记录 · 2026-08-07 · 个人中心显示北森/LeOrg ID

## 说明

北森任职 JSON 中身份 ID 为 `userID`（如 630702408）→ `beisen_user_id`；工号 `jobNumber`；LeOrg 同步主键为 `employee.id` → `leorg_emp_id`。

## 改动

- `user_dict` 增加 `leorg_emp_id`
- 个人中心「所属部门」卡展示：北森用户ID / 工号 / LeOrg 员工ID
