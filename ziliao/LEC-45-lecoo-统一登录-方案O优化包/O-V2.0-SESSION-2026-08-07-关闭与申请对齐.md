# 会话：账号、权限关闭与申请对齐

日期：2026-08-07

## 改动
- 关闭入口改用 `m-batch-grant` 大面板（选账号 / 关登录·权限 / 审批链预览）
- `account_close` 创建 `applications` + 步骤；待办类型统一「账号、权限关闭」
- `sensitive_close` 待办类型同步；`decide` 兼容旧「账号关闭」「敏感权限关闭」
- 文案统一顿号「账号、权限申请」

## 验证
- 徐好好提交关 OA 登录 → 待办类型「账号、权限关闭」、详情有 application 时间线
- 马宁通过 → `can_login=0`
