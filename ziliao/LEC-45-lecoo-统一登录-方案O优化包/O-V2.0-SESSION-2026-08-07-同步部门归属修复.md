# 会话记录 · 2026-08-07 · 同步部门误挂根导致直属/一级丢失

## 问题

清空后全量同步确认：徐好好等全员挂到根部门，直属马宁 / 一级吴锦志解析失败。

## 原因

预览时本地尚无部门映射，人员草稿 `dept_id` 落成根部门；确认 apply 未按 `org_id` 重算。

## 改动

- 草稿 payload 增加 `org_leorg_id`；apply 部门落库后按映射重算 `dept_id`
- `_realign_users_dept_from_leorg` 修复已落库错挂；直接同步后也会再对齐
- 已对现库重挂：徐好好 → BTIT，直属马宁，一级吴锦志
