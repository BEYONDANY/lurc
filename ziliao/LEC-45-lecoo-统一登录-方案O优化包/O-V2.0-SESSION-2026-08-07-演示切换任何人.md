# 会话记录 · 演示切换任何人（2026-08-07）

## 目标

顶部快速切换增加「切换任何人…」，按组织树筛选后切到任意通讯录人员。

## 改动摘要

- `app.py`：`GET /api/demo/org-pick`（免登录，含 admin；未筛选时截断前 80 人）
- `static/index.html`：顶部按钮打开选人弹窗；点「切换」调用 `switch-role`

## 手测

1. 刷新顶部栏 → 见「切换任何人…」
2. 左侧选组织 / 搜索姓名 → 点「切换」即登录该人
3. 可切到 admin（组织列显示「系统」）
