# 会话：审核操作备注

日期：2026-08-07

## 目标

审核相关操作（通过 / 驳回等）均可填写备注；流程详情时间线可见。

## 变更

1. `todos.remark`、`application_steps.remark` 字段 + 启动迁移
2. `POST /api/todo/<id>/decide` 接收 `remark`，落库；时间线返回 `remark`
3. 列表通过/驳回：备注弹窗；详情内可直接填备注后操作
4. 开通弹窗原有备注字段一并提交

## 补充（2026-08-07 晚）

列表通过/驳回曾因缺少 `#m-decide` 弹窗 DOM 导致 JS 报错、按钮像点不了。已补全备注确认弹窗。
