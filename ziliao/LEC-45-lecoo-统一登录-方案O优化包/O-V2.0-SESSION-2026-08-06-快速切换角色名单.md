# 会话记录 · 快速切换角色名单（2026-08-06）

## 目标

顶部「快速切换角色」换成指定人员，清掉旧演示角色按钮。

## 改动摘要

- `app.py`：`GET /api/demo-users` 固定返回：徐好好 / 高佳 / 马宁 / 吴锦志 / 李杨 / 常明明 / 超级管理员（`chenchao`）
- `static/index.html`：按钮文案改用 `button_label`（姓名或「超级管理员」）

## 手测

1. 刷新 http://127.0.0.1:5055
2. 确认顶部按钮为上述 7 项，无孙丽/张财/赵敏等旧项
3. 点击切换可登录对应用户
