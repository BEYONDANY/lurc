# 开发开工包 · 方案 O

本目录对齐方案 A「开发开工包」四件套，内容已按方案 O 裁剪。

| 文件 | 说明 |
|------|------|
| `01-接口契约-openapi-v1.yaml` | 治理 BFF + 开放 API；**OIDC 协议端点由 Logto 提供，不在本契约复刻** |
| `02-数据库设计-ddl-v1.sql` | 租户 / 成员 / 应用 / 权限目录 / 审计；**无密码哈希表** |
| `03-M1任务卡-backlog.md` | 对齐现有 lecoo 子任务（LEC-9~14）与硬化项 |
| `04-测试策略与关键用例.md` | 安全一票否决用例（移植 A，去掉 CAS） |

约定：变更契约需评审；无 FR 不做；禁止把 client_secret 写入仓库。
